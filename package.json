{
  "name": "serendipity-machine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The Serendipity Machine fleet — one random artwork from each museum's open collection.",
  "scripts": {
    "build": "node build-site.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "csv-parse": "^5.6.0"
  }
}
